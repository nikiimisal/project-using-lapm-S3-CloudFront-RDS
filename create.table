create database facebook;
Use facebook;
CREATE TABLE posts (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(20) NOT NULL,
    url VARCHAR(200),
    cfurl VARCHAR(200),
    caption VARCHAR(200)
);
